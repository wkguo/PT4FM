# PT4FM environment — validated on RTX PRO 6000 (Blackwell, CUDA 12.8), Python 3.11.
#
# Install torch (cu128) and jax (cuda13) FIRST from their CUDA wheels, THEN this file:
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128
#   pip install "jax[cuda13]==0.7.2"      # -> jaxlib / jax-cuda13-plugin / jax-cuda13-pjrt 0.7.2
#   pip install -r requirements.txt
#
# Editable repos (clone separately — see SETUP.md): openpi(fork, stage 4) and RLinf(stages 1-3).

# --- JAX stack (policy training, stage 4) ---
flax==0.10.2
optax==0.2.4
orbax-checkpoint==0.11.40
chex==0.1.90

# --- VLM / model ---
transformers==4.53.2
safetensors==0.8.0
sentencepiece==0.2.0
huggingface_hub==0.36.2
datasets==3.6.0

# --- LeRobot data + video ---
lerobot==0.3.3
av==17.1.0
pyarrow==24.0.0
pandas==2.2.3
numpy==2.4.4
pillow==12.2.0
opencv-python-headless==4.13.0.92
numpydantic==1.6.9
# torchcodec: MUST install from the CUDA-matching index, NOT plain PyPI (PyPI wheel is cu13 and
# fails with libnvrtc.so.13 not found on cu12 systems).  Install separately AFTER torch:
#   pip install --no-deps torchcodec --index-url https://download.pytorch.org/whl/cu128
# Validated: torchcodec-0.11.1+cu128 on torch-2.11.0+cu128 + FFmpeg 6.1 (libavcodec.so.60).
# When installed, lerobot auto-selects it as the default video backend (faster than PyAV).
# pt4fm.compat also ships a PyAV fallback (no-op if torchcodec is available).

# --- RLinf framework (value model + advantages, stages 1-3) ---
ray[default]>=2.47.0
hydra-core==1.3.3
omegaconf==2.3.1
tensordict==0.13.0
accelerate==1.14.0
draccus==0.10.0
easydict==1.13
cloudpickle==3.1.2

# --- misc ---
tyro==0.9.22
einops==0.8.1
etils==1.12.2
tqdm==4.68.3
tqdm-loggable==0.2
wandb==0.27.2
